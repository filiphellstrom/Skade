import { Text, View } from "react-native";

/**
 * Sida 1: Ny jaktdag - platshållare från scaffolding.
 * Ska: skapa en Jaktdag-rad direkt (datum + jaktmark), se
 * src/db/queries/jaktdag.ts -> skapaJaktdag(). Byggs i Sprint 1-chatten.
 */
export default function NyJaktdag() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Ny jaktdag</Text>
    </View>
  );
}
